spring.application.name=noc

# Server configuration
server.port=8081

# Database specific properties
spring.datasource.url=jdbc:mysql://localhost:3306/inven
spring.datasource.username=root
spring.datasource.password=Kalisari13790

# ORM software specific properties
spring.jps.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# If tables not exist, the table auto created with this config
# But if table exist, the table will overwrite!!!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER
